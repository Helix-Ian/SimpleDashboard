import React from 'react';

const ChartTitle = (props) => {
    
    var title = props.title;

    return (
        <div>
            <h3>{title}</h3>
        </div>
    )
};

export default ChartTitle;